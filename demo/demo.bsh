// Run this by running 'java -jar demo/dist/demo.jar'
import java.io.File;

for (File file : new File("lib").listFiles()) {
    if (file.getPath().endsWith(".jar")) {
        addClassPath(file.getAbsolutePath());
    }
}

addClassPath("dist/q.jar");

import com.on_site.q.Q;
